import {StyleSheet} from "react-native";

const styles =  StyleSheet.create({
    loginLabel: {
        fontSize: 30,
        textAlign: 'center',
        margin: 10,
        color: 'blue',
        fontWeight: 'bold',
    },
});

export default { styles }